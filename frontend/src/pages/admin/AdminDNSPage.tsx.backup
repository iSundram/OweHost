import { PlaceholderPage } from '../../components/PlaceholderPage';

export function AdminDNSPage() {
  return (
    <PlaceholderPage
      title="DNS Management"
      description="Complete DNS control and zone management"
      features={[
        'Zone management (create, delete, lock)',
        'DNS record management (A, AAAA, CNAME, MX, TXT, SRV)',
        'DNSSEC key management and rollover',
        'External DNS provider integration',
        'DNS sync state monitoring',
        'Zone file export/import',
      ]}
    />
  );
}
