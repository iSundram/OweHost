import { PlaceholderPage } from '../../components/PlaceholderPage';

export function ResellerDNSPage() {
  return (
    <PlaceholderPage
      title="DNS Management for Customers"
      description="Manage DNS zones and records for customer domains"
      features={[
        'Manage DNS zones for customer domains',
        'DNS record management (A, AAAA, CNAME, MX, TXT)',
        'DNS template creation',
        'Bulk DNS operations',
      ]}
    />
  );
}
